%%-*- mode: erlang -*-
%% ex: ft=erlang
{deps, [
        % Correct tagged IMEM is always pulled before dderloci
        % so this will not be pulled
        {imem, "", {git, "https://github.com/k2informatics/imem.git", master}},
        % TODO: Update to next release version od erloci
        {erloci, "", {git, "https://github.com/k2informatics/erloci.git", master}}
]}.
